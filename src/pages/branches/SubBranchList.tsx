import { Building2 } from "lucide-react"
import { ModulePlaceholder } from "@/components/shared"

export default function SubBranchListPage() {
  return (
    <ModulePlaceholder
      title="Sub Branches"
      description="Sub-units that roll up into a parent branch — departments, kiosks, sub-offices."
      icon={Building2}
    />
  )
}
