import { Building2 } from "lucide-react"
import { ModulePlaceholder } from "@/components/shared"

export default function BranchListPage() {
  return (
    <ModulePlaceholder
      title="Branches"
      description="Manage organisation branches — head office, regional offices, and outlets."
      icon={Building2}
    />
  )
}
